template Host "linux-host-snmp" {
  import "generic-service"

  vars.manubulon["cpu usage"] = {
    snmp_command = "snmp-load"
    snmp_warn = 80
    snmp_crit = 90
  }

  vars.manubulon["memory"] = {
    snmp_command = "snmp-memory"
    snmp_membuffer = true
  }

  vars.manubulon["disk /"] = {
    snmp_command = "snmp-storage"
    snmp_storage_name = "ˆ/$$"
  }
}
